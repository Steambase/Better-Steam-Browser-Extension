export type PackageReference = {
    id: string,
    steam_package_id: number,
    name?: string,
    slug: string,
    final_price?: number,
    page_image_url?: string,
    small_logo_url?: string,
};
